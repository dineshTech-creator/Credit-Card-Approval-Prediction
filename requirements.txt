# Runtime dependencies — pinned to match training environment exactly
# This prevents model unpickling errors due to library version mismatches
flask==3.0.3
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.5.0
xgboost==2.0.3
joblib==1.5.3
gunicorn==22.0.0
